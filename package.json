{
  "name": "svelte-app",
  "version": "1.0.0",
  "scripts": {
    "start": "sirv public --single",
    "clean": "rm -rf public/build",

    "prebuild": "echo '1. clean' && npm run clean",
    "build": "echo '2. build' && rollup -c",
    "postbuild": "echo '3. push sources' && git push origin sources",

    "preghPages": "echo '4. checkout master' && git checkout master",
    "ghPages": "echo '5. deploy gh-pages' && gh-pages -d public -b master",
    "postghPages": "echo '6. checkout sources' && git checkout sources",

    "dev": "rollup -c -w",

    "deploy": "npm run build && npm run ghPages"
  },
  "devDependencies": {
    "@rollup/plugin-commonjs": "11.0.2",
    "@rollup/plugin-node-resolve": "^7.0.0",
    "gh-pages": "^2.2.0",
    "rollup": "^1.20.0",
    "rollup-plugin-livereload": "^1.0.0",
    "rollup-plugin-svelte": "^5.0.3",
    "rollup-plugin-terser": "^5.1.2",
    "sass": "^1.26.5",
    "svelte": "^3.0.0",
    "svelte-preprocess": "^3.7.4"
  },
  "dependencies": {
    "sirv-cli": "^0.4.4"
  }
}
